<snippet>
  <content><![CDATA[
var fs = require('fs');
var ${1} = fs.readdirSync('./gulp/${1:tasks}');

${1}.forEach(function (${2:script}) {
  require('./${1}/' + ${2});
});
]]></content>
  <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
  <tabTrigger>gpimportscriptsinasubdirectory</tabTrigger>
  <!-- Optional: Set a scope to limit where the snippet will trigger -->
  <scope>text.plain, source.js</scope>
</snippet>
