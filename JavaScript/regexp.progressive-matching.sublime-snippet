<snippet>
  <content><![CDATA[
var ${2:regexp} = /${1:model(\.[\w.[\]]+)}/g;
var m;
while ((m = ${2}.exec(${3:str})) !== null) {
  var s = m[0];${0}
}
]]></content>
  <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
  <tabTrigger>regexp.progressive-matching, regexp.exec</tabTrigger>
  <!-- Optional: Set a scope to limit where the snippet will trigger -->
  <scope>source.js, text.plain</scope>
</snippet>
