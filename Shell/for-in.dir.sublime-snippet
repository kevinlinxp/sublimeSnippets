<snippet>
  <content><![CDATA[
for ${1:dir} in ${2:*}
do
  test -d "\$${1}" || continue
  ${3:echo "\$${1}"}
done
]]></content>
  <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
  <tabTrigger>for-in-dir</tabTrigger>
  <!-- Optional: Set a scope to limit where the snippet will trigger -->
  <scope>source.shell, text.plain</scope>
</snippet>