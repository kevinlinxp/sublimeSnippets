<snippet>
  <content><![CDATA[
<spring:hasBindErrors name="dto">
  <c:if test="${errors.hasFieldErrors('${1:username}')}">
    <span class="field-error">
      <c:out value="${errors.getFieldError('${1:username}').defaultMessage}"/>
    </span>
  </c:if>
</spring:hasBindErrors>
]]></content>
  <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
  <tabTrigger>jsp.spring.handle-validation-errors</tabTrigger>
  <!-- Optional: Set a scope to limit where the snippet will trigger -->
  <scope>text.html, text.html.jsp, text.plain</scope>
</snippet>
