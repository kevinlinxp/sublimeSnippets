<snippet>
  <content><![CDATA[
int prev;
do {
  prev = data.get();
  int next = // perform calculation
} while (!data.compareAndSwap(prev, next));
]]></content>
  <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
  <tabTrigger>java.concurrency.compare-and-swap.do-while</tabTrigger>
  <!-- Optional: Set a scope to limit where the snippet will trigger -->
  <scope>source.java, text.plain</scope>
</snippet>
