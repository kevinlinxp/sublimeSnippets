<snippet>
  <content><![CDATA[
for (;;) {
  int prev = data.get();
  int next = // perform calculation
  if (data.compareAndSet(prev, next)) {
    break;
  }
}
]]></content>
  <!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
  <tabTrigger>java.concurrency.compare-and-swap.for-loop</tabTrigger>
  <!-- Optional: Set a scope to limit where the snippet will trigger -->
  <scope>source.java, text.plain</scope>
</snippet>
