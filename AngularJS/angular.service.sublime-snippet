<snippet>
  <content><![CDATA[
(function () {
  'use strict';

  angular
    .module('${1:module}')
    .service('${2:ServiceId}', ${2});

  ${2}.\$inject = [${3/(\$(\w+)|\w+)/'$1'/g}];

  /* @ngInject */
  function ${2} (${3:dependencies}) {
    this.${4} = ${4};

    ////////////////

    function ${4:func} () {
    }
  }
})();
]]></content>
  <tabTrigger>ngservice</tabTrigger>
  <scope>text.plain, source.js</scope>
</snippet>
