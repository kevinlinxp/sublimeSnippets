<snippet>
  <content><![CDATA[
(function () {
  'use strict';

  angular
    .module('${1:module}')
    .factory('${2:ServiceId}', ${2}Factory);

  ${2}Factory.\$inject = [${3/(\$(\w+)|\w+)/'$1'/g}];

  /* @ngInject */
  function ${2}Factory (${3:dependencies}) {
    var service = {
      ${4}: ${4}
    };
    return service;

    ////////////////

    function ${4:func} () {
    }
  }
})();
]]></content>
  <tabTrigger>ngfactory</tabTrigger>
  <scope>text.plain, source.js</scope>
</snippet>
